using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrderCharacter : MonoBehaviour
{
    private void Awake()
    {
        Debug.Log("OrderCharacter");
    }
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("OrderCharacter");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
